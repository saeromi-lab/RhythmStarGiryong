{
  "name": "rhythm-star-giryong",
  "version": "1.0.0",
  "description": "경기대학교 실용음악학과 리듬 트레이닝 — 매일 배우고 메트로놈 연습",
  "type": "module",
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "start": "npm run dev"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
